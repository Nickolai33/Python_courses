1. Проверьте, чтобы хотя бы одно из чисел a или b оканчивались на 0.
Если такое существует, то проверить, существует ли треугольник
с такими сторонами(c - вводится отдельно с клавиатуры)
Если такое не существует, то проверить является ли оно чётным.
Если чётное, то вывести соответствующее сообщение, если нет,
то изменить значение переменной b на квадрат значения переменной a.

2. Решить квадратное уравнение

Функция sqrt() извлекает квадратный корень
from math import sqrt

Узнаем у пользователя, корни какого конкретного уравнения
мы будем искать.
a = float(input("a = "))
b = float(input("b = "))
c = float(input("c = "))

3. Определить принадлежность точки кругу с центром в начале координат.
Вводятся координаты (x,y) точки и радиус круга r.
Определить принадлежит ли данная точка кругу, если его центр находится
в начале координат.

from math import sqrt